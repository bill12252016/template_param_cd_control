version: '2.0'
services:
  app:
    restart: always
    build: ./
    logging:
      driver: json-file
      options:
        max-size: 50m
    ports:
      - "$HTTP_PORT:80"
    env_file:
      - .env
    command: nginx -g daemon off;
